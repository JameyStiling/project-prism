# Project Prism: Phase 1 TODO

- [x] Scaffold Project Structure (`core-engine`, `dashboard`, `drivers`).
- [x] Initialize Python virtualenv in `core-engine` and Vite/React/TS app in `dashboard`.
- [x] Create `GenericBaseDriver` abstract base class under `drivers/` per hardware factory standards.
- [x] Build WebSocket bridge between Python core and React dashboard (`core_engine.bridge` + `test_bridge.py`).
- [x] Create first TDD test for audio listener (`tests/test_audio.py`) and implement RMS capture with `sounddevice`.

